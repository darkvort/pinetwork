{
  "name": "pi-wallet",
  "version": "1.0.0",
  "description": "A project to send passphrases via email.",
  "main": "index.js",
  "scripts": {
    "start": "vercel dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nodemailer": "^6.9.1"
  },
  "devDependencies": {
    "vercel": "^28.16.11"
  }
}
